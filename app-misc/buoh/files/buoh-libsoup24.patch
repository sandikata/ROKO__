diff -ur buoh-0.8.2.orig/configure buoh-0.8.2/configure
--- buoh-0.8.2.orig/configure	2006-10-23 00:01:04.000000000 +0200
+++ buoh-0.8.2/configure	2011-02-02 10:51:25.389086832 +0100
@@ -23828,7 +23828,7 @@
 
 GLIB_REQUIRED=2.6.0
 GTK_REQUIRED=2.6.0
-LIBSOUP_REQUIRED=2.2.0
+LIBSOUP_REQUIRED=2.4.0
 GCONF_REQUIRED=2.2.0
 
 
@@ -23963,18 +23963,18 @@
         if test -n "$PKG_CONFIG" && \
     { (echo "$as_me:$LINENO: \$PKG_CONFIG --exists --print-errors \"glib-2.0       >= \$GLIB_REQUIRED
 		  gtk+-2.0       >= \$GTK_REQUIRED
-		  libsoup-2.2    >= \$LIBSOUP_REQUIRED
+		  libsoup-2.4    >= \$LIBSOUP_REQUIRED
 		  gconf-2.0      >= \$GCONF_REQUIRED\"") >&5
   ($PKG_CONFIG --exists --print-errors "glib-2.0       >= $GLIB_REQUIRED
 		  gtk+-2.0       >= $GTK_REQUIRED
-		  libsoup-2.2    >= $LIBSOUP_REQUIRED
+		  libsoup-2.4    >= $LIBSOUP_REQUIRED
 		  gconf-2.0      >= $GCONF_REQUIRED") 2>&5
   ac_status=$?
   echo "$as_me:$LINENO: \$? = $ac_status" >&5
   (exit $ac_status); }; then
   pkg_cv_BUOH_CFLAGS=`$PKG_CONFIG --cflags "glib-2.0       >= $GLIB_REQUIRED
 		  gtk+-2.0       >= $GTK_REQUIRED
-		  libsoup-2.2    >= $LIBSOUP_REQUIRED
+		  libsoup-2.4    >= $LIBSOUP_REQUIRED
 		  gconf-2.0      >= $GCONF_REQUIRED" 2>/dev/null`
 else
   pkg_failed=yes
@@ -23990,18 +23990,18 @@
         if test -n "$PKG_CONFIG" && \
     { (echo "$as_me:$LINENO: \$PKG_CONFIG --exists --print-errors \"glib-2.0       >= \$GLIB_REQUIRED
 		  gtk+-2.0       >= \$GTK_REQUIRED
-		  libsoup-2.2    >= \$LIBSOUP_REQUIRED
+		  libsoup-2.4    >= \$LIBSOUP_REQUIRED
 		  gconf-2.0      >= \$GCONF_REQUIRED\"") >&5
   ($PKG_CONFIG --exists --print-errors "glib-2.0       >= $GLIB_REQUIRED
 		  gtk+-2.0       >= $GTK_REQUIRED
-		  libsoup-2.2    >= $LIBSOUP_REQUIRED
+		  libsoup-2.4    >= $LIBSOUP_REQUIRED
 		  gconf-2.0      >= $GCONF_REQUIRED") 2>&5
   ac_status=$?
   echo "$as_me:$LINENO: \$? = $ac_status" >&5
   (exit $ac_status); }; then
   pkg_cv_BUOH_LIBS=`$PKG_CONFIG --libs "glib-2.0       >= $GLIB_REQUIRED
 		  gtk+-2.0       >= $GTK_REQUIRED
-		  libsoup-2.2    >= $LIBSOUP_REQUIRED
+		  libsoup-2.4    >= $LIBSOUP_REQUIRED
 		  gconf-2.0      >= $GCONF_REQUIRED" 2>/dev/null`
 else
   pkg_failed=yes
@@ -24023,12 +24023,12 @@
         if test $_pkg_short_errors_supported = yes; then
 	        BUOH_PKG_ERRORS=`$PKG_CONFIG --short-errors --errors-to-stdout --print-errors "glib-2.0       >= $GLIB_REQUIRED
 		  gtk+-2.0       >= $GTK_REQUIRED
-		  libsoup-2.2    >= $LIBSOUP_REQUIRED
+		  libsoup-2.4    >= $LIBSOUP_REQUIRED
 		  gconf-2.0      >= $GCONF_REQUIRED"`
         else
 	        BUOH_PKG_ERRORS=`$PKG_CONFIG --errors-to-stdout --print-errors "glib-2.0       >= $GLIB_REQUIRED
 		  gtk+-2.0       >= $GTK_REQUIRED
-		  libsoup-2.2    >= $LIBSOUP_REQUIRED
+		  libsoup-2.4    >= $LIBSOUP_REQUIRED
 		  gconf-2.0      >= $GCONF_REQUIRED"`
         fi
 	# Put the nasty error message in config.log where it belongs
@@ -24036,7 +24036,7 @@
 
 	{ { echo "$as_me:$LINENO: error: Package requirements (glib-2.0       >= $GLIB_REQUIRED
 		  gtk+-2.0       >= $GTK_REQUIRED
-		  libsoup-2.2    >= $LIBSOUP_REQUIRED
+		  libsoup-2.4    >= $LIBSOUP_REQUIRED
 		  gconf-2.0      >= $GCONF_REQUIRED) were not met:
 
 $BUOH_PKG_ERRORS
@@ -24050,7 +24050,7 @@
 " >&5
 echo "$as_me: error: Package requirements (glib-2.0       >= $GLIB_REQUIRED
 		  gtk+-2.0       >= $GTK_REQUIRED
-		  libsoup-2.2    >= $LIBSOUP_REQUIRED
+		  libsoup-2.4    >= $LIBSOUP_REQUIRED
 		  gconf-2.0      >= $GCONF_REQUIRED) were not met:
 
 $BUOH_PKG_ERRORS
diff -ur buoh-0.8.2.orig/configure.in buoh-0.8.2/configure.in
--- buoh-0.8.2.orig/configure.in	2006-10-22 23:59:48.000000000 +0200
+++ buoh-0.8.2/configure.in	2011-02-02 10:50:44.979104333 +0100
@@ -29,13 +29,13 @@
 
 GLIB_REQUIRED=2.6.0
 GTK_REQUIRED=2.6.0
-LIBSOUP_REQUIRED=2.2.0
+LIBSOUP_REQUIRED=2.4.0
 GCONF_REQUIRED=2.2.0
 
 PKG_CHECK_MODULES(BUOH, 
 		  glib-2.0       >= $GLIB_REQUIRED
 		  gtk+-2.0       >= $GTK_REQUIRED 
-		  libsoup-2.2    >= $LIBSOUP_REQUIRED
+		  libsoup-2.4    >= $LIBSOUP_REQUIRED
 		  gconf-2.0      >= $GCONF_REQUIRED) 
 AC_SUBST(BUOH_CFLAGS)
 AC_SUBST(BUOH_LIBS)
diff -ur buoh-0.8.2.orig/src/buoh-comic-loader.c buoh-0.8.2/src/buoh-comic-loader.c
--- buoh-0.8.2.orig/src/buoh-comic-loader.c	2006-10-21 16:56:37.000000000 +0200
+++ buoh-0.8.2/src/buoh-comic-loader.c	2011-02-02 10:51:15.651572983 +0100
@@ -202,7 +202,7 @@
 
 	proxy_uri = buoh_get_http_proxy_uri (BUOH);
 	if (proxy_uri) {
-		SoupUri *soup_uri = soup_uri_new (proxy_uri);
+		SoupURI *soup_uri = soup_uri_new (proxy_uri);
 
 		job->session = soup_session_sync_new_with_options (SOUP_SESSION_PROXY_URI, soup_uri, NULL);
 		
@@ -235,6 +235,7 @@
 
 static void
 buoh_comic_loader_job_read_next (SoupMessage        *msg,
+				 SoupBuffer         *chunk,
 				 BuohComicLoaderJob *job)
 {
 	gboolean success;
@@ -252,15 +253,14 @@
 						  msg->reason_phrase);
 		}
 
-		soup_message_set_status (msg, SOUP_STATUS_CANCELLED);
-		soup_session_cancel_message (job->session, msg);
+		soup_session_cancel_message (job->session, msg, SOUP_STATUS_CANCELLED);
 
 		return;
 	}
 
 	if (job->callback) {
-		job->callback (msg->response.body,
-			       msg->response.length,
+		job->callback (chunk->data,
+			       chunk->length,
 			       job->callback_data);
 	}
 }
@@ -275,9 +275,9 @@
 	msg = soup_message_new (SOUP_METHOD_GET, job->uri);
 	
 	soup_message_set_flags (msg, SOUP_MESSAGE_OVERWRITE_CHUNKS);
-	soup_message_add_handler (msg, SOUP_HANDLER_BODY_CHUNK,
-				  (SoupMessageCallbackFn)buoh_comic_loader_job_read_next,
-				  (gpointer) job);
+	g_signal_connect (msg, "got-chunk",
+			  G_CALLBACK (buoh_comic_loader_job_read_next),
+			  (gpointer) job);
 
 	buoh_debug ("resolving . . .");
 
